using System.Collections.Generic;
using UnityEngine;

public class ToggleBlueColor : MonoBehaviour
{
   /*  public DeadEyeColorGuy deadEyeColorGuy;

   
    private void Start()
    {
        deadEyeColorGuys = GetComponentsInChildren<DeadEyeColorGuy>();
    }

public void SpawnSwap()
{
    deadEyeColorGuys = GetComponentsInChildren<DeadEyeColorGuy>();
    if (deadEyeColorGuys.Length > 0)
    {
       

        foreach (DeadEyeColorGuy colorGuy in deadEyeColorGuys)
        {
            if (colorGuy != null)
            {
                if (!isBlue)
                {
                colorGuy.isBlue = isBlue;
                Debug.Log("ColorGuy");
                }
                else if (isBlue)
                {
                colorGuy.isBlue = !isBlue;
                Debug.Log("ColorGuy");
                }
            }
        }
    }
} */
}