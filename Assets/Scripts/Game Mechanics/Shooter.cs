using System;
using UnityEngine;

public class Shooter : MonoBehaviour
{


    // should be able to either take a mouse click an

    
    
    // make it a good tester, will need that a lot!
    // make it a good tester, will need that a lot!
    // own script that can keep track of the bullet and directions (maybe a struct)
    

    
    public void ShootBullet(Vector3 startPosition, Vector3 direction)
    {
        // shoot a ray cast and inform if an element was hit
        // IShootable
        
        // could return the Ishootable, so the gun can check if it hit the correct one (red or blue)
        // or return null if the first hit collider is not an Ishootable


      

        

    }



}